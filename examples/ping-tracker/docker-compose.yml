# Generated by container-secure-compose
# Source: examples\ping-tracker\app_config.yaml
#
# Do not edit this file directly. Regenerate with:
#   csc generate examples\ping-tracker\app_config.yaml

services:
  init:
    restart: 'no'
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
    image: ping-tracker-init:1.0.0
    volumes:
      - tracker-data:/data
  tracker:
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    user: nobody
    depends_on:
      - init
    read_only: true
    image: ping-tracker-tracker:1.0.0
    restart: unless-stopped
    tmpfs:
      - /tmp
    volumes:
      - tracker-data:/data
    networks:
      - app-internal
  ui:
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    user: nobody
    depends_on:
      - init
    read_only: true
    image: ping-tracker-ui:1.0.0
    ports:
      - 8080:8080
    restart: unless-stopped
    tmpfs:
      - /tmp
    volumes:
      - tracker-data:/data:ro
    networks:
      - app-internal
      - app-external
networks:
  app-internal:
    driver: bridge
    internal: true
  app-external:
    driver: bridge
volumes:
  tracker-data: {}
